# API Reference

The API reference is generated by MrDocs and published at:

- `reference/index.html`

Generate it locally with:

```bash
scripts/generate-api-documentation
```

Or build the full docs site (API reference + MkDocs) with:

```bash
scripts/build-docs
```
