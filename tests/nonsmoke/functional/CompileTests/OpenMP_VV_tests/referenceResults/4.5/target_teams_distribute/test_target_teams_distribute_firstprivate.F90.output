!$omp target data map(from:d(1:N)) map(to:a(1:N),b(1:N),c(1:N))
!$omp target teams distribute firstprivate(privatized,privatized_array) map(alloc:a(1:N),b(1:N),c(1:N),d(1:N)) num_teams(OMPVV_NUM_TEAMS_DEVICE)
!$omp end target data
!$omp target data map(from:d(1:N)) map(to:a(1:N),b(1:N),c(1:N))
!$omp target teams distribute firstprivate(privatized_array,privatized) map(alloc:a(1:N),b(1:N),c(1:N),d(1:N)) num_teams(OMPVV_NUM_TEAMS_DEVICE)
!$omp end target data
