// Reduced example problem from test2004_85.C (which includes #include<string>).

int* foo()
   {
     int*  __last;
     int* __first;
     int __val;

     switch (1) // __last - __first)
     {
     case 0:
     default:
       return __last;
     }
   }
