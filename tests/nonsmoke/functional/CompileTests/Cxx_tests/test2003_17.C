/* Example code from IRS */

typedef enum { MINIMUM_A } HOW_T_A;

typedef struct
   {
     int MINIMUM_B;
} HOW_T_B;

/* Also an error (debug later) */
enum { MINIMUM_C } value;

enum letters { a, b } Y;
