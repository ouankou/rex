# I don't think this directory can ever be reached.
# We only descend into OpenMP if we're not building with OFP-enabled Fortran support.
# We only descend into this directory if we are building with Fortran support.
# Fortran OFP still requires the JVM.
