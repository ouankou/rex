# ROSE Test Harness config file

subdir = yes

cmd = ${VALGRIND} "${EXE}" -rose:verbose 0 -c "${srcdir}/test1A.C"
cmd = ${VALGRIND} "${EXE}" -rose:verbose 0 -c "${srcdir}/test1B.C"
cmd = g++ -o test1 test1A.o test1B.o
cmd = ./test1
