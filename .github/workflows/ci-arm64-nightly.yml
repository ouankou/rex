name: Nightly ARM64 CI

on:
  schedule:
    # Every day at 14:00 UTC (07:00 PDT)
    - cron: "0 14 * * *"
  workflow_dispatch:

concurrency:
  group: nightly-arm64-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test-arm64:
    runs-on: ubuntu-26.04

    defaults:
      run:
        shell: bash

    env:
      IMAGE_REPO: ghcr.io/${{ github.repository_owner }}/rex
      LOCAL_IMAGE_REPO: rex-local
      TARGET_ARCH: arm64
      TARGET_PLATFORM: linux/arm64

    steps:
    - name: Set up QEMU (${{ env.TARGET_ARCH }})
      uses: docker/setup-qemu-action@v4
      with:
        platforms: ${{ env.TARGET_ARCH }}

    - name: Run core tests from prebuilt image (${{ env.TARGET_ARCH }} via QEMU)
      if: ${{ !env.ACT }}
      run: |
        set -euo pipefail
        docker pull --platform="${TARGET_PLATFORM}" "${IMAGE_REPO}:dev"
        docker run --rm --platform="${TARGET_PLATFORM}" \
          "${IMAGE_REPO}:dev" \
          bash -lc '
            set -euo pipefail
            cd /opt/rex/src
            CTEST_JOBS="$(nproc)"
            ctest --test-dir build \
              --no-tests=error \
              -R "rex|astInterface|OMPLOWERING_RODINIA_" \
              --output-on-failure \
              -j"${CTEST_JOBS}"
          '

    - name: Run core tests from local dev image (${{ env.TARGET_ARCH }} via QEMU)
      if: ${{ env.ACT }}
      run: |
        set -euo pipefail
        docker run --rm --platform="${TARGET_PLATFORM}" \
          "${LOCAL_IMAGE_REPO}:dev-${TARGET_ARCH}-local" \
          bash -lc '
            set -euo pipefail
            cd /opt/rex/src
            CTEST_JOBS="$(nproc)"
            ctest --test-dir build \
              --no-tests=error \
              -R "rex|astInterface|OMPLOWERING_RODINIA_" \
              --output-on-failure \
              -j"${CTEST_JOBS}"
          '
